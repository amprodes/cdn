export declare class DavChipList {
  /**
   * Indicates whether the chips should be wrapped.
   * @prop {boolean} wrapChips - Set to `true` to wrap the chips, `false` otherwise. Defaults to `false`.
   */
  wrapChips: boolean;
  render(): any;
}
